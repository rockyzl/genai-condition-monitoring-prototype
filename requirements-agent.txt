# Optional agent dependencies — the LangGraph engine lane ONLY.
#
# The base install (requirements.txt) and the default hand-written "native"
# engine never import these, so the Streamlit Space stays lean. Install only to
# run `--engine langgraph`:
#
#     .venv/bin/pip install -r requirements-agent.txt
#
# The LangGraph engine runs the SAME registry tools, gates, cards, and grounded
# answerer as the native engine — deterministic nodes, no LLM in the loop. See
# docs/langgraph-engine.md for the mapping and the checkpoint/resume split.
langgraph>=1.0,<2.0
